根据中文名生成名字头像

```rust
avatar_generator.exe --name "张三" --output "张三.png"
```

![张三](./zhangsan.png)

```text
Avatar image creator 1.0
Paddi

USAGE:
    avatar_generator.exe [OPTIONS]

OPTIONS:
    -h, --help               Print help information
    -n, --name <NAME>        Sets the name of the person
    -o, --output <OUTPUT>    Sets the output png file
    -s, --size <SIZE>        Sets the width of the square image [default: 100]
    -V, --version            Print version information
```